# File: .env.example

BOT_TOKEN=
CLIENT_SECRET=
CLIENT_ID=
TRACKING_CHANNEL_ID=
MUSE_API_SEARCH_ENDPOINT=
MUSE_API_MUSIC_ENDPOINT=

# PostgreSQL Database (Prisma v7)
# Recommended for most uses (with connection pooling)
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# For uses requiring a connection without pgbouncer
DATABASE_URL_UNPOOLED=postgresql://user:password@host:port/database?sslmode=require

# Prisma-specific connection string (with connection timeout)
POSTGRES_PRISMA_URL=postgresql://user:password@host:port/database?connect_timeout=15&sslmode=require